
<CH.Code>
```typescript ListDrinks.ts
import { Speakeasybar } from "speakeasybar";
import { DrinkType } from "speakeasybar/models/components";

async function run() {
  const sdk = new Speakeasybar();

  const drinkType = DrinkType.Wine;
  
  const res = await sdk.drinks.listDrinks(drinkType);

  if (res?.statusCode !== 200) {
    throw new Error("Unexpected status code: " + res?.statusCode || "-");
  }
  
  // handle response
}

run();
```
---

```json Example Response
  {
    "code": "string",
    "details": {},
    "message": "string"
  }
```
</CH.Code>
