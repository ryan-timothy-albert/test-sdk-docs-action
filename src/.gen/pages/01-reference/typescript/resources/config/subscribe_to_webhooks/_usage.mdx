
<CH.Code>
```typescript SubscribeToWebhooks.ts
import { Speakeasybar } from "speakeasybar";
import { Webhook } from "speakeasybar/models/operations";

async function run() {
  const sdk = new Speakeasybar({
    apiKey: "<YOUR_API_KEY_HERE>",
  });

  const result = await sdk.config.subscribeToWebhooks([
    {},
  ]);

  // Handle the result
  console.log(result)
}

run();
```
---

```json Example Response
  {
    "code": "string",
    "details": {},
    "message": "string"
  }
```
</CH.Code>
