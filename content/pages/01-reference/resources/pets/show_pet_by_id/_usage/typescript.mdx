
import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "200"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```typescript ShowPetById.ts
  import { Speakeasybar } from "speakeasybar";
import { ShowPetByIdRequest } from "speakeasybar/dist/models/operations";

(async() => {
  const sdk = new Speakeasybar();
const petId: string = "string";

  const res = await sdk.pets.showPetById(petId);

  if (res.statusCode == 200) {
    // handle response
  }
})();
  ```
  ---

  ```json Example Response
  {
    "id": 592845,
    "name": "string",
    "tag": "string"
  }
  ```
  </CH.Code>
</WithStatusBar>
