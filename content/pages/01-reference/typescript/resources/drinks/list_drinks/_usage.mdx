
<CH.Code>
```typescript ListDrinks.ts
import { Speakeasybar } from "speakeasybar";
import { DrinkType } from "speakeasybar/models/components";

const speakeasybar = new Speakeasybar();

async function run() {
  const drinkType = DrinkType.Wine;
  
  const result = await speakeasybar.drinks.listDrinks(drinkType);

  // Handle the result
  console.log(result)
}

run();
```
---

```json Example Response
  {
    "code": "None",
    "details": {},
    "message": "None"
  }
```
</CH.Code>
