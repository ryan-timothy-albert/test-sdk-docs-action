
<CH.Code>
```typescript CreateOrder.ts
import { Speakeasybar } from "speakeasybar";
import { OrderType } from "speakeasybar/models/components";

const speakeasybar = new Speakeasybar({
  apiKey: "<YOUR_API_KEY_HERE>",
});

async function run() {
  const requestBody = [
    {
      productCode: "NAC-3F2D1",
      quantity: 715190,
      type: OrderType.Ingredient,
    },
  ];
  const callbackUrl = "<value>";
  
  const result = await speakeasybar.orders.createOrder(requestBody, callbackUrl);

  // Handle the result
  console.log(result)
}

run();
```
---

```json Example Response
  {
    "code": "None",
    "details": {},
    "message": "None"
  }
```
</CH.Code>
